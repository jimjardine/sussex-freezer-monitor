# Installed into the venv (created with --system-site-packages by setup_pi.sh).
# The GPIO backend (RPi.GPIO on Bullseye, lgpio on Bookworm) comes from apt and
# is seen via system-site-packages — we don't pip-build it here.
requests>=2.25
gpiozero>=1.6
