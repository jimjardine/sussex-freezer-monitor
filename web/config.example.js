// Copy to config.js and fill in. These are PUBLIC values (safe in the browser):
// the anon key only grants access governed by Row Level Security + login.
window.SUPABASE_CONFIG = {
  url: "https://YOUR-PROJECT.supabase.co",
  anonKey: "YOUR_ANON_KEY",
};
